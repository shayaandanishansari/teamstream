/* The shared drive. Replaced in Phase 5 once the file service exists. */
export function Drive() {
  return (
    <main className="stub">
      <p className="label">Drive</p>
      <h1>The shared drive</h1>
      <p className="stub-note">
        Not wired yet — the file service it reads from is the next thing built.
      </p>
    </main>
  );
}
